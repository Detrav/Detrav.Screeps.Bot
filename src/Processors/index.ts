// import * as harvester from "./Harvester";
// import * as builder from "./Builder";

// const result: CreepProcessors = {};

// function copyProcessors(from: CreepProcessors, to: CreepProcessors) {
//   for (let key in from) {
//     to[key] = from[key];
//   }
// }

// copyProcessors(harvester.default, result);
// copyProcessors(builder.default, result);

// export const processors = result;
